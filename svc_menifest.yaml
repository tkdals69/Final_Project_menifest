apiVersion: v1
kind: Service
metadata:
  name: flask-service
spec:
  selector:
    app: flask
  ports:
  - protocol: TCP
    port: 5000          # 외부에서 접속할 포트 (원하면 80, 8080 등 변경 가능)
    targetPort: 5000    # 컨테이너 내부에서 서비스되는 포트
  type: NodePort 
